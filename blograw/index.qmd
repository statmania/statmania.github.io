---
# title: "Stat Mania"
# subtitle: "Making Sense of Statistics"
listing:
  contents: posts
  sort: "date desc"
  type: grid
  categories: cloud
  image-placeholder: img/log.jpg
  sort-ui: true
  filter-ui: true
  feed: true
  fields: [image, date, title, author, reading-time, categories, description]
page-layout: full
title-block-banner: true
---

```{=html}
<style>
  /* Hide the default Quarto title block on the blog homepage; we render our own hero. */
  #title-block-header { display: none !important; }
  #quarto-content > main.content { padding-top: 0 !important; }
</style>

<div class="sm-hero-wrap">
  <canvas id="sm-canvas"></canvas>
  <div class="sm-hero">
    <span class="sm-eyebrow">Statistics &middot; Mathematics &middot; Data Science</span>
    <h1 class="sm-title">Stat Mania Blog</h1>
    <p class="sm-subtitle">Tutorials, statistical theory, and dev notes — written to make sense of data, one post at a time.</p>
  </div>
</div>

<script>
(function(){
  var canvas = document.getElementById('sm-canvas');
  if (canvas && canvas.getContext) {
    var ctx = canvas.getContext('2d');
    var wrap = canvas.parentElement;
    var W, H, dpr;
    var particles = [];
    var reduceMotion = window.matchMedia && window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    var count = Math.min(70, Math.floor((window.innerWidth || 1200) / 18));

    function resize(){
      dpr = Math.min(window.devicePixelRatio || 1, 2);
      W = wrap.clientWidth;
      H = wrap.clientHeight;
      canvas.width = W * dpr;
      canvas.height = H * dpr;
      canvas.style.width = W + 'px';
      canvas.style.height = H + 'px';
      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
    }

    function init(){
      particles = [];
      for (var i = 0; i < count; i++) {
        particles.push({
          x: Math.random() * W,
          y: Math.random() * H,
          vx: (Math.random() - 0.5) * 0.25,
          vy: (Math.random() - 0.5) * 0.25,
          r: Math.random() * 1.6 + 0.6
        });
      }
    }

    function step(){
      ctx.clearRect(0, 0, W, H);
      var maxDist = 130;
      for (var i = 0; i < particles.length; i++) {
        var p = particles[i];
        p.x += p.vx; p.y += p.vy;
        if (p.x < 0 || p.x > W) p.vx *= -1;
        if (p.y < 0 || p.y > H) p.vy *= -1;

        for (var j = i + 1; j < particles.length; j++) {
          var q = particles[j];
          var dx = p.x - q.x, dy = p.y - q.y;
          var dist = Math.sqrt(dx * dx + dy * dy);
          if (dist < maxDist) {
            ctx.strokeStyle = 'rgba(0,229,255,' + (0.16 * (1 - dist / maxDist)) + ')';
            ctx.lineWidth = 1;
            ctx.beginPath();
            ctx.moveTo(p.x, p.y);
            ctx.lineTo(q.x, q.y);
            ctx.stroke();
          }
        }
      }
      for (var k = 0; k < particles.length; k++) {
        var pt = particles[k];
        ctx.beginPath();
        ctx.fillStyle = 'rgba(180,225,255,0.85)';
        ctx.arc(pt.x, pt.y, pt.r, 0, Math.PI * 2);
        ctx.fill();
      }
      if (!reduceMotion) requestAnimationFrame(step);
    }

    resize();
    init();
    step();
    window.addEventListener('resize', function(){ resize(); init(); });
  }
})();
</script>
```
