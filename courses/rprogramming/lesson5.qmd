---
title: "5. Data Frames"
description: "Learn R data frames — the table type most real statistics and data analysis in R is built on — with live, editable code."
engine: knitr
filters:
  - webr
webr:
  show-startup-message: false
---

A data frame is R's table type — a set of equal-length columns, each possibly a different type. This is what most real statistics and data analysis in R is built on.

```{webr-r}
students <- data.frame(
  name  = c("Ada", "Grace", "Rosalind"),
  score = c(92, 88, 95),
  pass  = c(TRUE, TRUE, TRUE)
)

students
summary(students$score)
students[students$score > 90, ]
```

Previous: [4. Control Flow](lesson4.qmd) &nbsp;&middot;&nbsp; Next: [6. Plotting](lesson6.qmd)
