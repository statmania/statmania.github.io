---
title: "1. Variables & Basic Types"
description: "Learn R variable assignment and basic types (numeric, character, logical) with live, editable R code."
engine: knitr
filters:
  - webr
webr:
  show-startup-message: false
---

In R, you create a variable with the assignment arrow `<-` (an equals sign `=` also works). R has a few core types: numeric, character (text), and logical (`TRUE`/`FALSE`). Use `class()` to check a value's type. Edit the code below, then press **Run Code**.

```{webr-r}
age <- 25
name <- "Ada"
is_student <- TRUE

age
class(age)
class(name)
```

Next: [2. Vectors](lesson2.qmd)
